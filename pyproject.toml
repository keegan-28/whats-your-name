[project]
name = "whats_your_name"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.2.1",
    "ray>=2.47.1",
    "torch>=2.5.1",
]

[dependency-groups]
dev = [
    "ruff>=0.12.1",
]
